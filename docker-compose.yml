services:
  sparkmon:
    build: .
    container_name: sparkmon
    ports:
      - "8095:80"
    restart: unless-stopped

  sparkmon-mcp:
    build: ./mcp
    container_name: sparkmon-mcp
    ports:
      - "8097:8096"
    restart: unless-stopped
